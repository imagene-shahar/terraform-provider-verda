# Verda API credentials for integration testing
# Copy this file to .env and fill in your credentials
# NEVER commit .env files with real credentials!

# Required for integration tests
VERDA_CLIENT_ID=your-client-id
VERDA_CLIENT_SECRET=your-client-secret

# Optional: Override Terraform/OpenTofu command
# TF_CMD=tofu

# Optional: Skip expensive tests
# SKIP_INSTANCE_TEST=1
# SKIP_CONTAINER_TEST=1
# SKIP_SERVERLESS_JOB_TEST=1

# Optional: Enable tests for resources still in testing stage
# RUN_REGISTRY_CREDENTIALS_TEST=1

# Run all tests in sequence
# RUN_ALL_TESTS=1

# =============================================================================
# Configurable Test Variables
# =============================================================================
# Override instance configuration for testing in different environments.
# These map to Terraform variables via the TF_VAR_* pattern.
# Defaults (from docs): instance_type=1B200.30V, image=ubuntu-24.04-cuda-12.8-open-docker, location=FIN-03

# Example: Use staging instance type (RTX PRO 6000)
# TF_VAR_instance_type=1RTXPRO6000.30V

# Example: Use different image
# TF_VAR_instance_image=ubuntu-24.04-cuda-12.8-open-docker

# Example: Use different location
# TF_VAR_instance_location=FIN-03
